'use strict';

function findKeyIndex (arr, item) => {

    if(typeof key === 'undefined') throw new TypeError;
    if(typeof key !== 'array') throw new TypeError;
    if(arr.length === 0) return NaN;
    for(let i = arr.length; i > math.ceiling(arr.length/2; i--){
        arr[i] = arr[i-1];
        if(arr[i] !== key) {
            arr[i] = arr[i-1]
        } else if {
            (arr[i] === key) {
                console.log(i); //will show index of modified array, not original
                for(let i = arr.length; i > math.ceiling(arr.length/2; i--){
                    arr[i] = arr[i-1];
                    if(arr[i] !== key) {
                        arr[i] = arr[i-1]
                    } else if {
                        (arr[i] === key) {
                            return i; //will show index of original array
                        }
            }
        }
    };

};