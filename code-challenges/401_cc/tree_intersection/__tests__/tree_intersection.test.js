'use strict';

//work in progress
// const treeIntersection = require('../tree-intersection.js');

// xdescribe('treeIntersection', () => {
//   it('returns a tree', () => {
//   let arr = [1, 3, 5, 7];
// let newArr = [];
// for (let i = arr.length -1; i >= 0; i --) {
//     newArr.push(arr[i]);
//   }
//   return newArr;
// expect(newArr).toBe(7, 5, 3, 1);
//   });
// };
