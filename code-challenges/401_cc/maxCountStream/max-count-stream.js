'use strict';

//in process

const getMaxCount = stream => {
  let temp = stream.getNext();
  let hashmap = new Map();
  hashmap.add(temp);
  heap.insert();

  for(let i = 0, i < n, i++){
    let max = heap.extract();
  }
}