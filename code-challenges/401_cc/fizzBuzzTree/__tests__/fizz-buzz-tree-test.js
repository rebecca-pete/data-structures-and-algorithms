'use strict';

const {fizzBuzzTree, Node} = require('../fizzp-buzz-tree.js');

describe('node', () => {
  it('is a number', () => {
    let node = new Node();
    if(node.val === number)
    expect(node instanceof Node).toBeTruthy();
  });
