'use strict';

let fizz = 3;
let buzz = 5;
let fizzbuzz = 10;
let node;
class Node {
    constructor(val){
        this.node = node.val;
        this.leftChild = node.leftChild;
        this.rightChild = node.rightChild;
    }
}

function FB(node){
    if(node === null) return;
    if(typeOf (node.val === number)){
        //logic
        if((node.val % 3 === 0) && (node.val % 5 === 0)) {
            node.val = 'FizzBuzz';
        } else if {
            (node.val % 3 === 0) {
                node.val = 'Fizz';
            } else {
                (node.val % 5 === 0){
                    node.val = 'Buzz';
                }
            }
        }
        //recursive calls
        if(node.leftChild !== null){
            function FB(node.leftChild){
                if(node.rightChild !== null){
                    function FB(node.rightChild);
                }
            }
        }
    }
}